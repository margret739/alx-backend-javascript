/**
 * Creates a set from an array.
 * @param {Array} array - The sorce array.
 * @author Bezaleel Olakunori <https"//github.com/B3zaleel>
 * @returns {set}
 */
export default function setFromArray(array) {
  return new Set(array);
}
